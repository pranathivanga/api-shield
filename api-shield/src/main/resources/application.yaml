spring:
  application:
    name: api-shield
  redis:
    host: localhost
  datasource:
    url: jdbc:h2:mem:apishield
    driverClassName: org.h2.Driver
    username: sa
    password: ""

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true